# تحديث الوكلاء v5 — 3 خانات عربية ثابتة + تحقّق إلزامي بعد كل عملية

**ملف واحد فقط تغيّر: `js/agents.js` (استبدال كامل).**

---

## العطل المُبلَّغ عنه

حذف "0x3" من داخل المشروع (v4) لم ينعكس عند `app.hyperliquid.xyz/API` — بقي ظاهراً هناك. v4 كانت تُصدّق `status:'ok'` من الخادم وحده كدليل نجاح كافٍ، مع منطق ديناميكي (عدّاد تسلسلي + "خانات مُحرَّرة" محلياً + تفادي تصادم أجهزة) لاختيار الاسم. هذا التعقيد جعل أي فشل صامت بآلية "إعادة الاعتماد بنفس الاسم" (لا بديل موثّق لها) صعب الاكتشاف.

## الحل: تبسيط + تحقّق فعلي، لا تخمين إضافي

### 1) 3 خانات عربية ثابتة دائماً: `وكيل-1`، `وكيل-2`، `وكيل-3`
لا عدّاد، لا "أسماء مُحرَّرة" مُتتبَّعة محلياً، لا احتمال تصادم بين أجهزة. نفس 3 الأسماء دائماً — تُستعلَم حالتها من `extraAgents` مباشرة كل مرة. كل خانة تُعرض دوماً:
- **فارغة** (لم تُستخدَم، أو منتهية فعلياً حسب الخادم) → زر **⚡ تفعيل** خاص بها.
- **نشطة، نملك مفتاحها** (أنشأها هذا المتصفح) → 🔗 رابط · 🔑 المفتاح · 🗑 حذف.
- **نشطة، لا نملكه** (من جهاز/مصدر آخر بنفس الاسم) → 🗑 حذف فقط، مع وسم "🌐 من مكان آخر".

زر "إنشاء جديد" العام حُذف تماماً (بديل أبسط طلبته مباشرة) — بما أن هناك دائماً 3 بطاقات بالضبط، لا حاجة لملاحظة "الحد الأقصى 3" منفصلة؛ البنية نفسها توضّح ذلك.

أي وكيل آخر مرتبط بحسابك (بلا اسم، أو باسم غير هذه الثلاثة — من الموقع الرسمي، جهاز آخر، أو إصدار سابق لهذا المشروع) يظهر بقسم منفصل أسفل الخانات الثلاث، قابلاً للحذف من هنا أيضاً.

### 2) ✅ تحقّق فعلي إلزامي بعد كل إنشاء/حذف — الإصلاح الجوهري

لم نعد نُصدّق `status:'ok'` وحده. بعد أي توقيع `approveAgent` (تفعيل خانة أو حذف وكيل)، يُعاد استعلام `extraAgents` فعلياً — **حتى 3 محاولات بفواصل 600ms** (مراعاةً لزمن انتشار الحالة بالخادم) — للتأكد أن العنوان/الاسم أصبحا كما يُفترَض حقاً:
- **تفعيل**: يتأكد أن `وكيل-N` أصبح فعلاً يحمل عنوان المفتاح الجديد.
- **حذف**: يتأكد أن العنوان المحذوف لم يعد مرتبطاً بذلك الاسم عند الخادم إطلاقاً.

لو فشل التحقّق رغم قبول التوقيع، يظهر **خطأ صريح** للمستخدم بدل نجاح وهمي — بالضبط ما كان يجب أن يظهر بالعطل المذكور لو كان هذا التحقّق حاضراً حينها. هذا سيكشف فوراً بالمحاولة القادمة هل المشكلة زالت، أو ما زالت قائمة (وعندها الخطأ نفسه سيوضّح ذلك بدل نجاح مضلِّل).

**بصراحة تامة**: الآلية المستخدمة (إعادة اعتماد نفس الاسم بمفتاح عشوائي مُهمَل) هي الوحيدة الموثّقة رسمياً لإبطال وكيل مبكراً (لا إجراء "إلغاء" منفصل بكامل واجهة Hyperliquid — تأكَّد هذا مباشرة من exchange-endpoint ومن الكود المصدري الرسمي لـPython SDK). لو استمر ظهور نفس الخطأ بعد هذا التحديث، فهذا دليل قوي أن آلية المطابقة بالاسم عند Hyperliquid تعمل بشكل مختلف عمّا توثّقه الصفحات العامة، ويستحق فتح تذكرة دعم فني معهم مباشرة بهذه النقطة تحديداً — لا مجالاً أوسع للتخمين من طرفنا بلا اختبار مباشر ضد حساب حقيقي.

## فحوصات ما قبل التسليم
`node --check` (نجح)، صفر إشارات متبقية لأي رمز من التصميم الديناميكي القديم (v4)، صفر تكرار IDs، تحقّق يدوي من توازن الأقواس (الفارق الظاهر بعدّ نصّي خام مصدره نص عربي بين قوسين داخل جمل الواجهة، لا خللاً برمجياً — مؤكَّد عبر فحص node نفسه).
