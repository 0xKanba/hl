# تحديث الوكلاء v3 — حذف حقيقي بتوقيع + بلا حد + ترقيم 0x

**ملف واحد فقط تغيّر: `js/agents.js` (استبدال كامل). لا حاجة لمس أي ملف آخر — الوكلاء ذاتية الحقن بالكامل (CSS/HTML) كما كانت.**

---

## 1) الثغرة الحقيقية: الحذف السابق كان محلياً بحتاً

`revokeOne()` بالإصدار القديم كانت تمسح السجل من `localStorage` فقط — **بلا أي اتصال شبكة إطلاقاً**. النتيجة: الوكيل "المحذوف" يبقى مفوَّضاً وفعّالاً بالكامل عند Hyperliquid. أي نسخة من مفتاحه الخاص (لو تسرّبت، أو صُدِّرت لجهاز آخر قبل الحذف) تقدر تتداول نيابة عن الحساب رغم أن التطبيق "نسي" الوكيل تماماً. هذا لا يطابق موقع Hyperliquid الرسمي، الذي يطلب توقيعاً فعلياً عند إزالة وكيل ويُبطله حقاً من جهته.

## 2) الآلية الحقيقية — بحث مباشر، لا افتراض

فُحص توثيق `exchange-endpoint` الرسمي مباشرة + الكود المصدري لـ Hyperliquid Python SDK الرسمي (`hyperliquid-dex/hyperliquid-python-sdk`, `exchange.py`). النتيجة المؤكَّدة:

- **لا يوجد أي إجراء "إلغاء/إبطال" منفصل بكامل واجهة Hyperliquid.** `approveAgent` هي الإجراء الوحيد المتعلق بالوكلاء بكل الـ API — لا `revokeAgent`، لا `deregisterAgent`.
- الآلية الحقيقية الوحيدة لإبطال وكيل مبكراً (قبل انتهاء صلاحيته الطبيعية): **إعادة اعتماد نفس اسم الخانة** بمفتاح عشوائي جديد يُهمَل فوراً بلا حفظ. Hyperliquid يستبدل حامل الاسم السابق بهذا الجديد — سلوك مُجرَّب فعلياً بهذا المشروع تاريخياً (راجع CHANGES.md القديم: "Hyperliquid يستبدل الوكيل صاحب نفس الاسم عند أي approveAgent جديد بنفس الاسم").
- توثيق `exchange-endpoint` يؤكد أيضاً: حساب واحد يملك وكيلاً غير مُسمّى واحداً + **حتى 3 وكلاء مُسمّين فقط** كحد أقصى، بلا أي إجراء لحجز اسم جديد كلياً بعد امتلاء الثلاثة — إعادة استخدام **نفس** اسم قديم هي الطريقة الوحيدة لتحرير سعة فعلية.

`_revokeSigned()` الجديدة تنفّذ هذا بالضبط: توقيع واحد من محفظتك الرئيسية (نفس تدفق الإنشاء)، يُبطل الوكيل فعلياً عند Hyperliquid — لن يعود صالحاً للتداول بأي مكان بعدها، بصرف النظر عن مكان بقاء مفتاحه الخاص.

## 3) بلا حد + ترقيم 0x1، 0x2... 0x10... بلا تراجع أبداً

بما أن الاسم الحقيقي المُرسَل لـ Hyperliquid مقيَّد فعلياً بـ 3 خانات (نقطة 2 أعلاه)، هذه الخانات الثلاث (`market-liq-1/2/3`) تبقى **كما هي بالضبط** داخلياً — غير مرئية للمستخدم إطلاقاً، ولضمان توافق خلفي كامل مع أي وكيل مُوافَق عليه فعلاً قبل هذا التحديث.

الجديد: **رقم تسلسلي محلي بحت** (`0x1`، `0x2`، `0x3`... `0x10`...) منفصل تماماً عن الاسم الحقيقي، مخزَّن بـ `hl_agent_seq_<addr>`:
- يزيد فقط عند **نجاح إنشاء حقيقي** — محاولة أُلغيت أو فشلت لا تستهلك رقماً (بلا فجوات).
- **لا يتراجع أبداً**، حتى لو حُذفت كل الوكلاء — بالضبط كما طُلب: `0x9` يُحذف ← التالي `0x10`، لا `0x1` من جديد.
- بما أن الحذف الآن حقيقي، السعة الحقيقية (3 خانات) تتحرر تلقائياً مع كل حذف — فلا حد "مصطنع" يظهر بالاستخدام الطبيعي (إنشاء ← حذف ← إنشاء...). الرسالة "لديك 3 وكلاء فعّالين الآن" تظهر فقط لو امتلك المستخدم 3 وكلاء **صالحين بنفس اللحظة فعلاً** — حالة حقيقية نادرة، لا حداً افتراضياً مسبقاً.

وكيل أي مستخدم موجود أصلاً (بلا اسم معروض بعد) يُعطى رقماً تسلسلياً تلقائياً أول تحميل بعد هذا التحديث — بلا أي فعل مطلوب منه.

## 4) تبسيط جذري للواجهة

بطاقة كل وكيل الآن 3 أزرار فقط: **🔗 رابط تداول · 🔑 كشف المفتاح · 🗑 حذف**. حُذف تماماً:
- تصدير/استيراد JSON بالكامل (تنزيل ملف، لصق نص، من ملف) — لم تعد موجودة.
- التفعيل اليدوي ("🎯 نشّطه") — **تلقائي بالكامل الآن**: أحدث وكيل صالح محلياً هو المُستخدَم دائماً للتوقيع، بلا أي إعداد يدوي أو حالة "مفضَّل" مخزَّنة.
- صندوق عنوان المحفظة داخل نافذة الوكلاء — متوفر أصلاً بنافذة "الإيداع"، لا داعي لتكراره هنا.

## 5) بلا تغيير — عزل المحافظ

كل شيء (السجل المحلي + العداد التسلسلي) يبقى مفتاحاً بعنوان المحفظة (`hl_agents_<addr>` / `hl_agent_seq_<addr>`)، تماماً كالإصدار السابق — فلا تسريب أو تداخل بين حسابات/محافظ مختلفة بنفس المتصفح.

## فحوصات ما قبل التسليم

`node --check` (نجح)، فحص آلي لعدم وجود أي إشارة متبقية للرموز/المعرّفات المحذوفة (`MAX_NAMED_AGENTS`, `_getPref/_setPref`, `exportJSON/importJSON/copyJSONText`, `agImportBtn/agPasteArea`... إلخ — نظيف تماماً)، عدم تكرار أي `id=` بالـ HTML المحقون، وتوازن الأقواس `{}`/`()`.
