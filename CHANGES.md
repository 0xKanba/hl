# إعادة هيكلة "صفحة واحدة" + صفحة فتح صفقة جديدة

**جديد:** `css/order.css` · `js/order/` (6 ملفات)
**مُستبدَل:** `index.html` · `js/app.js` · `js/auth.js` · `js/trading.js` · `js/state.js` · `js/prices.js` · `css/components.css` · `sw.js`

---

## المبدأ المعماري

`index.html` صار **مُحمِّلاً فقط** — وسوم `<link>` و`<script>` وهيكل الصدفة الثابت، لا أكثر. أي واجهة جديدة من الآن = **ملف JS يبنيها + ملف CSS ينسّقها**، صفر HTML مضاف. نفس نموذج `tradingview.com/chart`.

طُبِّق هذا فعلياً بهذه الدفعة على شيئين:

| | قبل | بعد |
|---|---|---|
| شريط الرئيسية | `<button class="new-order-bar">` بـindex.html | `js/order/bar.js` يبنيه |
| تنسيق شاشة التداول | سلاسل نصية داخل `ui.js` تُحقن وقت التشغيل | `css/order.css` — ملف تنسيق حقيقي |

استخراج CSS تم **آلياً** من المصفوفة القديمة (104 قاعدة) لا بإعادة كتابة يدوية — صفر انحراف.

## الشريط

بانر الضيف القديم ("🔒 اربط محفظتك لبدء التداول وعرض صفقاتك المفتوحة / اتصال ←") **حُذف نهائياً** — دالّتاه `_showGuestBanner`/`_hideGuestBanner` ومواضع استدعائهما الأربعة وتنسيقه بـ`components.css`.

بدلاً منه شريط واحد دائم أعلى بطاقة الرصيد:

```
فتح صفقة جديدة                          [ فتح ]
```

نقرة بأي مكان عليه (أو Enter/Space) تفتح الشاشة. الضيف لا يفقد التوجيه: `OrderModule.open()` تستدعي `_promptConnect()` — توست + نبض زر الاتصال. نية المستخدم أولاً، ثم ما يلزم لتحقيقها، بدل حاجز قبل أن يطلب شيئاً أصلاً.

## التوقيت والتزامن

أربعة تحسينات قابلة للقياس، لا تجميل:

**1. تجميع بإطار واحد (rAF).** نبضات BBO تصل عدة مرات بالثانية، وكل واحدة كانت تُعيد بناء المعاينة بالكامل (`innerHTML`) فوراً. الآن كل المصادر (نبضة سعر، كتابة بحقل، تبديل جهة/نوع) تمر عبر `OM._scheduleRefresh()` ← تحديث واحد لكل إطار. السلاسة تبقى 60fps مهما تسارع السوق، والتأخير المُضاف أقل من 17ms.

**2. كتابة مشروطة.** السعر الكبير لا يُكتب إن كان النص مطابقاً لما هو معروض أصلاً — كل كتابة تعني إعادة تخطيط محتملة بلا فائدة.

**3. كاش عمق لكل عملة.** `l2Book` تُدفَع بكل بلوك مضى عليه نصف ثانية أو أكثر. فتح الشاشة أو تبديل الأصل كان ينتظر تلك الدفعة بلوحة باهتة؛ الآن يُرسَم فوراً من آخر لقطة محفوظة، وتُستبدل بأول دفعة حيّة.

**4. بناء مسبق وقت الخمول.** `bar.js` يبني هيكل الشاشة ويربط أحداثها عبر `requestIdleCallback` (بمهلة قصوى 2.5 ثانية) بعد استقرار الإقلاع — فأول نقرة تفتح شاشة جاهزة بدل بنائها لحظتها.

بالإضافة: الرسم يتوقف كلياً وقت إغلاق الشاشة (الاشتراك يبقى ليملأ الكاش فقط)، و`fetchSessionStats` للأصل المختار تُطلق بالتوازي بلا انتظار.

## منطق الأمر (بلا تغيير عن الدفعة السابقة)

| الجهة | السعر مقابل السوق | البنية المُرسَلة |
|---|---|---|
| شراء | أعلى | `trigger{isMarket:true, triggerPx, tpsl:'sl'}` |
| شراء | أقل | `limit{tif:'Gtc'}` |
| بيع | أقل | `trigger{…}` |
| بيع | أعلى | `limit{tif:'Gtc'}` |

TP/SL مع سوق/حدّي تُحزَم بتوقيع واحد (`grouping:'normalTpsl'`). مع أمر الإيقاف تُخزَّن محلياً (معزولة بعنوان المحفظة) وتُلحَق تلقائياً فور التفعيل الفعلي — التحقق عبر ظهور الأمر في `fillsCache`، لا مجرد اختفائه من `openOrders`.

**لا حد أدنى لقيمة الأمر** — الشرط الوحيد `qty > 0`، مطابق للنظام القديم.

## فحوصات ما قبل التسليم

`node --check` على كل ملفات JS · توازن أقواس CSS · صفر تكرار IDs بـindex.html · صفر بقايا لـ`new-order-bar`/`guest-banner`/`newOrderBar`/`injectCSS` · تطابق كل `getElementById('om…')` مع معرّف مبني فعلاً.
