# تحديث الوكلاء v2.1 — ماركت ليك

## ⚠️ إصلاح جذري (v2.1) — "حدث خطأ غير متوقع" عند إنشاء وكيل

**السبب الحقيقي، مؤكَّد بفحص آلي:** `_confirmApproval` بـ`js/agents.js` (وبالنسخة الأصلية قبله أيضاً) كانت تعتمد على نافذة تفويض ثابتة متوقَّعة بـ`index.html`
(`modalAgentApproval` / `agentAddrPreview` / `agentApprovalConfirm` / `agentApprovalCancel`) — **غير موجودة فعلياً** بالملف. عند محاولة إنشاء وكيل:
`$('agentApprovalConfirm').onclick = ...` كانت تُنفَّذ على `null`، فترمي `TypeError: Cannot set properties of null` فوراً — قبل حتى فتح نافذة توقيع المحفظة. هذا الخطأ التقني لا يطابق أي كلمة إنجليزية بـ`errToAr`، فيسقط لعبارتها الافتراضية العامة **"حدث خطأ غير متوقع — حاول مجدداً"** بالضبط.

**الإصلاح:** نافذة التفويض (`#agApproveModal`) أصبحت **ذاتية الحقن بالكامل** من `agents.js` نفسه — بلا أي اعتماد على `index.html` — تماماً كبقية نوافذ هذا الملف. فُحص آلياً بعد الإصلاح: **كل** معرّف (`id`) يستخدمه `agents.js` مُعرَّف داخله هو نفسه (صفر اعتماد خارجي)، **وصفر** نداءات DOM غير محصَّنة متبقية بالملف (`getElementById(...).prop = ` بلا `?.` أو فحص وجود مسبق).

**تحسين مصاحب بـ`js/utils.js`:** `errToAr()` كانت تستبدل *أي* رسالة عربية جاهزة (حتى الواضحة تماماً) بالعبارة العامة لمجرد عدم مطابقتها كلمة إنجليزية — الآن تُمرَّر أي رسالة عربية كما هي. هذا يُصلح المشكلة جذرياً لأي كود مستقبلي بالمشروع كله يرمي رسالة عربية جاهزة، لا لهذا الملف فقط.

**جديد أيضاً بهذي الجولة:** تصدير/استيراد الوكيل الآن بطريقتين متكافئتين لكل اتجاه — 💾 تنزيل ملف + 📋 نسخ كنص (تصدير)، 📥 من ملف + 📋 لصق نص JSON عبر `textarea` (استيراد) — لسهولة الإرسال/اللصق بواتساب أو تيليجرام مباشرة بلا تعامل مع مرفقات.

**مراجعة "التنقل بين المحافظ":** فُحصت مسارات `auth.js` (بريد Privy / محفظة خارجية / رابط وكيل / إلغاء اتصال) — كل حالة معزولة بمفاتيح `localStorage` منفصلة **لكل عنوان محفظة على حدة** (`hl_agents_<addr>`، `hl_agent_pref_<addr>`)، فلا تسريب بيانات وكيل من محفظة لأخرى عند التبديل — لم يُعثر على أي خلل هنا، لا تغيير مطلوب.

---

**8 ملفات فقط تغيّرت/أُضيفت — انسخها فوق نفس المسارات بالمستودع (لا تلمس أي ملف آخر).**

```
index.html          (مُعدَّل)
sw.js               (مُعدَّل)
css/agentlink.css   (جديد)
js/agentlink.js     (جديد)
js/agents.js        (استبدال كامل — v2)
js/app.js           (مُعدَّل)
js/auth.js          (مُعدَّل)
js/utils.js         (مُعدَّل)
```

---

## 1) تاريخ الانتهاء غير المطابق لموقع Hyperliquid — root cause

السبب الحقيقي: التطبيق كان يحسب `createdAt + 180 يوم` محلياً فقط، بمعزل عن أي بيانات خادم. الآن:

- كل `approveAgent` يُرفق صراحة `valid_until <ms>` داخل `agentName` نفسه (موثّق رسمياً، حد أقصى 180 يوماً — هذا هو أصل رقم الـ180 أساساً، لم يكن اختراعاً محلياً).
- كل عرض/فحص صلاحية يمر عبر `type:"extraAgents"` (info request رسمي يُرجع `[{name,address,validUntil}]` — **نفس البيانات بالضبط** التي تعرضها صفحة API بموقع Hyperliquid).

النتيجة: تطابق حرفي مضمون، لأن التطبيقين يقرآن نفس المصدر — لا تخميناً محلياً بعد الآن.

## 2) وكيلان بجهازين، توقيع الأول يُسقط الثاني بصمت — root cause

السبب: كل الأجهزة كانت تستخدم **نفس الاسم الثابت** `suyula-agent`. Hyperliquid يستبدل الوكيل صاحب نفس الاسم عند أي `approveAgent` جديد بنفس الاسم (موثّق رسمياً). جهاز ثانٍ بمحفظة فارغة محلياً كان يُنشئ وكيلاً بنفس الاسم فيُسقط وكيل الجهاز الأول دون أن يشعر أي منهما.

الحل: أسماء مرقّمة `market-liq-1/2/3` (Hyperliquid يسمح بحد أقصى 3 وكلاء مُسمّين لكل حساب). قبل إنشاء أي وكيل، يُستعلَم `extraAgents` الحقيقي أولاً ويُختار أول اسم **غير محجوز فعلياً على الخادم** — فلا يتصادم جهازان أبداً طالما بقيت خانة حرة من الثلاث.

**لا "تدوير" بعد الآن** (كان يعيد استخدام اسم فيُسقط غيره) — فقط إنشاء/حذف صريحان بحرية كاملة، كما طُلب حرفياً.

## 3) كشف المفتاح + رابط تداول + JSON

عند إنشاء أي وكيل (أو بالضغط على "🔑 المفتاح" لاحقاً من قائمته): يظهر مفتاحه الخاص كاملاً (قابل للنسخ)، مع:
- **🔗 نسخ رابط تداول** — نفس آلية Hyperliquid الرسمية (`?link=base64({address,key})`) عبر `js/agentlink.js` الجديد. يعمل لموقعنا افتراضياً؛ `AgentLink.build(addr, pk, 'hyperliquid')` يبني رابط `app.hyperliquid.xyz/trade` الرسمي نفسه لو احتجته لاحقاً.
- **💾 تنزيل JSON** — لنقل الوكيل لجهاز آخر بلا توقيع جديد. زر "📥 استيراد JSON" بنفس النافذة يقرأه.

## 4) js/agentlink.js — استهلاك `?link=`

عند فتح `hl.kanba.pw/?link=...` بجهاز/متصفح جديد كلياً: `app.js` (أول سطر بتسلسل الإقلاع، قبل Privy/EXTWALLET) يستدعي `AgentLink.tryConsume()` — لو الرابط صالح، جلسة تداول فورية بلا أي توقيع (`State.agent` = المفتاح مباشرة، `State.wallet` = كائن قراءة فقط `isAgentLink:true`). لا تُحفَظ الجلسة محلياً (أمان: لا يبقى مفتاح وكيل بجهاز غريب). أي محاولة إيداع/سحب/إدارة وكلاء/تصدير تُحجَب برسالة عربية واضحة (`_blockIfAgentLink` بـ`app.js`، `errToAr` بـ`utils.js`).

## 5) روابط المستكشف

أسفل زر "🔌 إلغاء الاتصال" بـ`addr-popover` (نقر عنوان الحساب بالـappbar): رابطا **🔍 المستكشف (Blockscan)** و **📊 الحالة (Hyperscreener)**، يُبنيان ديناميكياً من عنوان المحفظة الحالي (`auth.js:_updateExplorerLinks`)، ويفتحان بنفس التبويب (بلا `target="_blank"`) — تماماً كرابط "📄 الوثائق والدعم" بالدرج، الذي أُزيل منه `target="_blank"` أيضاً لنفس السبب.

## توافق خلفي — لا كسر لأي مستخدم حالي

وكيل أي مستخدم مُوافَق عليه أصلاً بالصيغة القديمة (`hl_agent_<addr>`) يُرحَّل تلقائياً لأول تحميل بعد النشر (`_migrateV1` بـ`agents.js`) — بنفس اسمه/مفتاحه، بلا أي توقيع جديد مطلوب. `Agents.ensure()` / `Agents.getInfo()` / `Agents.revoke()` تحتفظ بنفس التوقيع الخارجي المستخدَم من `api.js`/`auth.js` — لا حاجة لتعديلهما.

## فحوصات ما قبل التسليم

`node --check` على الملفات الستة (نجح)، توازن IDs/وسوم HTML بـ`index.html` (نجح، بلا تكرار)، سطر `<script src="/js/agentlink.js">` بالترتيب الصحيح (بعد wallets.js، قبل utils.js)، `sw.js` مُحدَّث (APP_SHELL + رفع نسخة الكاش).
